# vk-static-host

Static hosting for HTML5 game builds published to VK Mini Apps (and similar platforms
that only accept a URL, without their own hosting). Each game gets its own subfolder,
deployed as-is from `library/tools/build.py` output in the main GameDev repo.

Not the game source — just the built, playable files.
