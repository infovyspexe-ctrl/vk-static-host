// ЧИСЛА РЕКЛАМЫ. Политика — в замкнутом модуле `meta/adGate.js` (копия
// `library/mechanics/ad-gate/`), здесь только значения этой игры.
//
// Лимиты на СУТКИ по каждой точке: вознаграждаемая должна оставаться наградой, а не фоном.
// Ключи — это же имена в сейве (`ads.adCounts`); опечатка в ключе на стороне сцены сделала
// бы точку неработающей молча, поэтому canReward для незнакомого ключа возвращает false.
export const REWARD_LIMITS = {
  secondWind: 2,    // «Второе дыхание» — подняться после смерти в бою
  extraCard: 6,     // ещё одна карта в награде за бой
  restHeal: 4,      // выспаться у костра (лечит вдвое больше)
  x2memory: 3,      // удвоить память в конце похода
};

// Сколько ИГРОВОГО времени копится до взвода полноэкранной.
export const INTERSTITIAL_ARM_SEC = 170;

// Сколько походов игрок должен ЗАВЕРШИТЬ, прежде чем увидит первую полноэкранную.
// Требование «реклама не до конца обучения» (пункт C6 RELEASE-CHECKLIST.md).
export const INTERSTITIAL_MIN_RUNS = 1;

// Награда за ежедневный дар по дням серии (в памяти). Циклится.
export const DAILY_REWARDS = [15, 20, 25, 30, 40, 50, 70];
