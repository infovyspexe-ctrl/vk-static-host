// ЧИСЛА РЕКЛАМЫ. Политика показов — в core/adGate.js, здесь только значения.
//
// Лимиты на СУТКИ по каждой точке: rewarded должен оставаться наградой, а не фоном.
// Ключи — это же имена в meta.adCounts; опечатка в ключе на стороне сцены сделала бы точку
// неработающей молча, поэтому AdGate.canReward для незнакомого ключа возвращает false.
export const REWARD_LIMITS = {
  x2crystals: 5,   // удвоить кристаллы за забег (экран смерти)
  reroll: 6,       // пересдать карты апгрейда
  revive2: 3,      // второе воскрешение за забег
  startKit: 3,     // стартовый набор перед забегом
  chest: 4         // второй сундук после босса (появится в Вехе 2)
};

// Сколько ИГРОВОГО времени копится до взвода интерстишла. Взвод != показ: показ случается
// в ближайшей логической паузе (экран смерти), а не по срабатыванию таймера в бою —
// «голый таймер» Яндекс называет антипримером (YANDEX-SDK.md).
export const INTERSTITIAL_ARM_SEC = 150;

// Сколько партий игрок должен ЗАВЕРШИТЬ, прежде чем увидит первый интерстишл.
// Требование «реклама не до конца обучения» (пункт C6 RELEASE-CHECKLIST.md).
export const INTERSTITIAL_MIN_RUNS = 1;

// Сколько рероллов выбора апгрейда даётся ЗА ОДИН ЗАБЕГ (сверх суточного лимита).
export const REROLLS_PER_RUN = 2;
